#!/usr/bin/python3
def search_replace(my_list, search, replace):
    fresh_list[]
    for i in range(0, len(my_list)):
        if fresh_list[i] == search:
            new.append(replace)
        else:
            new.append(fresh_list[i])
        return fresh_list
