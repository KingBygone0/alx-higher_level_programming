#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    new = [] * 3
    new_matrix = [new] * len(new)
    for i in range(0, len(matrix)):
        new = []
        for j in range(0, len(new_matrix[i]))
        a = new_matrix[i][j] ** 2
        new.append(a)
    new_matrix.append(new)
    return new_matrix
